#include<stdio.h>
void main(){

	int x;
	
	printf("Enter the no. of array:\n");
	scanf("%d",&x);

	int arr[x];
	int y,a,e,i,o,u ;

	printf("Enter array elements:\n");

	for(y=0;i<x;i++){
	
		scanf("%d",&arr[i]);
	}

	printf("Index of the vowel consuming elements:\n");

	for(y=0;i<x;i++){
	
		if(arr[i]=a,
		   arr[i]=e,
	   	   arr[i]=i,
		   arr[i]=o,
		   arr[i]=u);
	

	printf("%d",arr[i]);
	}
}
